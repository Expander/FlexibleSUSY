
#ifndef @ModelName@_HIGH_SCALE_CONSTRAINT_H
#define @ModelName@_HIGH_SCALE_CONSTRAINT_H

#include "inputPars.hpp"

class @ModelName@;

class @ModelName@_high_scale_constraint {
public:
   @ModelName@_high_scale_constraint(const @ModelName@_input_parameters&);
   virtual ~@ModelName@_high_scale_constraint();
   virtual void apply();
   virtual double get_scale() const;
   virtual void set_model(@ModelName@*);

private:
   double scale;
   @ModelName@* model;
   @ModelName@_input_parameters inputPars;

   void update_scale();
};

#endif
